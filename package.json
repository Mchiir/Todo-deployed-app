{
  "name": "to-do",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build":"if-env TYPE=server && (cd server && npm install) || (cd client && npm install)",
    "dev":"nodemon ./server/server.js",
    "client":"node client && npm start"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "if-env":"^1.0.4"
  }
}
